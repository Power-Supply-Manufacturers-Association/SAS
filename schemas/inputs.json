{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "http://openconverters.com/schemas/SAS/inputs.json",
    "$comment": "SPDX-License-Identifier: Apache-2.0",
    "title": "inputs",
    "description": "The description of the inputs that can be used to design or evaluate a Semiconductor. Mirrors the MAS / CAS / RAS inputs structure (operatingPoints[] + designRequirements). Operating points use the PEAS multiPortOperatingPoint template since semiconductors are typically three- or four-terminal devices (gate/drain/source, anode/cathode, base/collector/emitter).",
    "type": "object",
    "properties": {
        "operatingPoints": {
            "description": "Data describing the operating points",
            "title": "operatingPoints",
            "type": "array",
            "items": {
                "$ref": "../../PEAS/schemas/inputs/multiPortOperatingPoint.json"
            },
            "minItems": 1
        },
        "designRequirements": {
            "description": "Data describing the design requirements",
            "title": "designRequirements",
            "$ref": "./inputs/designRequirements.json"
        }
    },
    "required": [
        "operatingPoints",
        "designRequirements"
    ],
    "$defs": {}
}
